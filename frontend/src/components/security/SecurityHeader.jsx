const SecurityHeader = () => {
return (
    <div className="settings-header">
        <div>
            <h1 className="settings-title">Security & Sign-in</h1>
            <p className="settings-subtitle">Manage your password, two-factor authentication, and active sessions.</p>
        </div>
    </div>
);
};

export default SecurityHeader;
