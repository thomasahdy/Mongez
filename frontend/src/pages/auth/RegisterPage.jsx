import RegisterContainer from "../../components/auth/register/RegisterContainer";

const RegisterPage = () => {
  return (
    <main className="min-h-screen flex items-center justify-center px-5 py-10 bg-bg-body animate-fadeIn">
      <RegisterContainer />
    </main>
  );
};

export default RegisterPage;